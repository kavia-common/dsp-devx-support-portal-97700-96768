# Development environment variables for React app
# PUBLIC_INTERFACE
# Backend API base URL used by the frontend services. Defaults to http://localhost:3001 if not set.
REACT_APP_API_BASE_URL=http://localhost:3001
